# Fonts

This directory needs a local handwriting font for the Chinese demo.

Current local development uses:

- `PingFangShiGuang.ttf`

The font file is not committed because this repository is public and the
redistribution rights should be confirmed before publishing the binary font.
Place the file here locally before building `device/riddle`.

